apiVersion: v1
kind: Pod
metadata:
  name: devboard
spec:
  containers:
    - name: devboard-frontend
      image: devboard-frontend:latest
      imagePullPolicy: IfNotPresent
      ports:
        - containerPort: 4173
